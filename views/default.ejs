<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= page.title %></title>
    <meta name="description" content="<%= page.description %>">
    <meta property="og:title" content="<%= page.title %>">
    <meta property="og:description" content="<%= page.description %>">
    <meta property="og:url" content="<%= pageUrl %>">
    <meta property="og:type" content="website">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            line-height: 1.6;
            color: <%= page.textColor %>;
            background: <%= page.backgroundColor %>;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        .hero {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            position: relative;
            background: linear-gradient(135deg, <%= page.backgroundColor %>, <%= page.accentColor %>15);
        }

        <% if (page.heroImage) { %>
        .hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('<%= page.heroImage %>') center/cover no-repeat;
            opacity: 0.1;
            z-index: -1;
        }
        <% } %>

        .hero-content {
            max-width: 800px;
            z-index: 1;
        }

        .hero h1 {
            font-size: 3.5rem;
            font-weight: 700;
            margin-bottom: 1rem;
            background: linear-gradient(135deg, <%= page.textColor %>, <%= page.accentColor %>);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .hero h2 {
            font-size: 1.5rem;
            font-weight: 300;
            margin-bottom: 2rem;
            opacity: 0.8;
        }

        .hero p {
            font-size: 1.2rem;
            margin-bottom: 3rem;
            opacity: 0.7;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
        }

        .cta-button {
            display: inline-block;
            background: <%= page.accentColor %>;
            color: white;
            padding: 16px 32px;
            text-decoration: none;
            border-radius: 50px;
            font-weight: 600;
            font-size: 1.1rem;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px <%= page.accentColor %>40;
        }

        .cta-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px <%= page.accentColor %>60;
        }

        .features {
            padding: 80px 0;
            background: <%= page.backgroundColor %>;
        }

        .features-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 40px;
            margin-top: 60px;
        }

        .feature {
            text-align: center;
            padding: 40px 20px;
            background: white;
            border-radius: 12px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.1);
            transition: transform 0.3s ease;
        }

        .feature:hover {
            transform: translateY(-5px);
        }

        .feature-icon {
            width: 60px;
            height: 60px;
            background: <%= page.accentColor %>;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 20px;
            color: white;
            font-size: 24px;
        }

        .footer {
            background: <%= page.textColor %>;
            color: <%= page.backgroundColor %>;
            text-align: center;
            padding: 40px 0;
        }

        @media (max-width: 768px) {
            .hero h1 {
                font-size: 2.5rem;
            }
            
            .hero h2 {
                font-size: 1.2rem;
            }

            .hero p {
                font-size: 1rem;
            }

            .features-grid {
                grid-template-columns: 1fr;
                gap: 20px;
            }
        }

        .powered-by {
            position: fixed;
            bottom: 20px;
            right: 20px;
            background: rgba(0,0,0,0.8);
            color: white;
            padding: 8px 16px;
            border-radius: 20px;
            font-size: 12px;
            z-index: 1000;
        }
    </style>
</head>
<body>
    <main class="hero">
        <div class="container">
            <div class="hero-content">
                <h1><%= page.title %></h1>
                <h2><%= page.subtitle %></h2>
                <p><%= page.description %></p>
                <a href="<%= page.ctaUrl %>" class="cta-button">
                    <%= page.ctaText %>
                </a>
            </div>
        </div>
    </main>

    <section class="features">
        <div class="container">
            <h2 style="text-align: center; font-size: 2.5rem; margin-bottom: 20px; color: <%= page.textColor %>;">
                Why Choose Us?
            </h2>
            <p style="text-align: center; font-size: 1.2rem; opacity: 0.7; max-width: 600px; margin: 0 auto;">
                Discover what makes us different and why thousands of customers trust us.
            </p>
            
            <div class="features-grid">
                <div class="feature">
                    <div class="feature-icon">🚀</div>
                    <h3 style="margin-bottom: 15px; color: <%= page.textColor %>;">Fast & Reliable</h3>
                    <p style="opacity: 0.7;">Lightning-fast performance with 99.9% uptime guarantee.</p>
                </div>
                
                <div class="feature">
                    <div class="feature-icon">🛡️</div>
                    <h3 style="margin-bottom: 15px; color: <%= page.textColor %>;">Secure & Safe</h3>
                    <p style="opacity: 0.7;">Enterprise-grade security to protect your data and privacy.</p>
                </div>
                
                <div class="feature">
                    <div class="feature-icon">💡</div>
                    <h3 style="margin-bottom: 15px; color: <%= page.textColor %>;">Smart Solutions</h3>
                    <p style="opacity: 0.7;">Intelligent features that adapt to your needs automatically.</p>
                </div>
            </div>
        </div>
    </section>

    <footer class="footer">
        <div class="container">
            <p>&copy; <%= new Date().getFullYear() %> Generated Landing Page. All rights reserved.</p>
            <p style="margin-top: 10px; font-size: 0.9rem; opacity: 0.7;">
                Created: <%= new Date(page.createdAt).toLocaleDateString() %> | 
                Views: <%= page.views %>
            </p>
        </div>
    </footer>

    <div class="powered-by">
        Powered by Landing Page API
    </div>

    <script>
        // Add smooth scrolling for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // Add loading animation
        document.addEventListener('DOMContentLoaded', function() {
            document.body.style.opacity = '0';
            document.body.style.transition = 'opacity 0.5s ease-in-out';
            setTimeout(() => {
                document.body.style.opacity = '1';
            }, 100);
        });
    </script>
</body>
</html>